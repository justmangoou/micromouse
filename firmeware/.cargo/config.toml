[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = 'probe-rs run --chip STM32F103C8 --protocol swd --connect-under-reset --chip-erase'

[build]
target = "thumbv7m-none-eabi"

[env]
DEFMT_LOG = "trace"
